// Dummy test file for template repo
// This will be removed during finalise-setup
describe('template', () => {
  it('should be replaced with real tests', () => {
    expect(true).toBe(true);
  });
});
